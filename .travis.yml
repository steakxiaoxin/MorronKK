language: node_js
node_js:
  - 'stable'
cache: yarn

install:
  - yarn install

script:
  - yarn run build

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  target_branch: gh_pages # default target branch gh_pages
  on:
    branch: master
